import { ArrowRight, Leaf, ShieldCheck, UserRound } from 'lucide-react'; import type { DemoUser } from '../types'; import { Badge, Button, Card } from './ui';
const roleCopy={admin:'Full organization visibility',manager:'Department-level decisions',employee:'Personal participation view'};
export function DemoAccess({users,onSelect}:{users:DemoUser[];onSelect:(userId:string)=>void}){return <main className="access-page"><section className="access-intro"><div className="access-brand"><span className="brand-mark"><Leaf size={17}/></span>EcoSphere</div><Badge tone="green">Phase 2 · Demo access</Badge><h1>One ESG story.<br/>Three ways to act.</h1><p>Select a seeded persona to explore the same trusted organization from the right level of responsibility.</p><div className="access-note"><ShieldCheck size={17}/><span><b>Demo-safe access</b><small>Local data only · Resets with your browser storage</small></span></div></section><section className="persona-panel"><p className="eyebrow">CHOOSE A PERSONA</p><h2>Enter Atlas Industries</h2><p className="persona-description">Each profile has role-aware access prepared for the workflows in the next phases.</p><div className="persona-list">{users.map((user)=><Card key={user.id} className="persona-card"><span className="persona-avatar">{user.initials}</span><div><div className="persona-top"><h3>{user.name}</h3><Badge tone={user.role==='admin'?'green':'neutral'}>{user.role}</Badge></div><p>{user.title}</p><small>{roleCopy[user.role]}</small></div><Button aria-label={`Enter as ${user.name}`} onClick={()=>onSelect(user.id)}><ArrowRight size={17}/></Button></Card>)}</div><p className="access-footer"><UserRound size={14}/>No password needed for this seeded demo environment.</p></section></main>}
